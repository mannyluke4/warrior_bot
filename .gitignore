# Secrets — NEVER push
.env
# Note: .env.engine TEMPLATE is committed (with <FILL_IN> placeholders);
# do NOT commit the live-creds variant. If you want the live variant kept
# locally only, rename it .env.engine.local and update loaders.

# Python
venv/
venv_new/
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Logs (large, regenerable)
logs/
*.log

# Cached data
l2_cache/
tick_cache/
tick_cache_engine/

# Ephemeral state
last_session_symbols.txt
watchlist.txt

# OpenClaw/Duffy identity files
SOUL.md
AGENTS.md
TOOLS.md
IDENTITY.md
USER.md
HEARTBEAT.md
MEMORY.md
BOOTSTRAP.md

# Node artifacts
node_modules/
package-lock.json

# Scanner results (regenerable)

# Full profile backtest outputs (regenerable)
full_profile_backtest_outputs/
full_profile_backtest_results.csv
full_profile_backtest_log.txt

# Session/status docs (ephemeral)
.openclaw/
*_ISSUE_*.md
*_STATUS_*.md
*_CONSTRAINTS_*.md
*_GAPS_*.md
SESSION_*.md
nohup.out
